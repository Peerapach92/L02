services:
  nginx:
    image: "nginx:stable-bullseye"
    ports:
      - "8000:80"
    volumes:
      - ./www:/usr/share/nginx/html
      - ./nginx_default.conf:/etc/nginx/conf.d/default.conf
  fast-api:
    build: .
    image: ${DOCKER_HU}
    expose:
      - 8000
  redis:
    image: "redis:alpine"
